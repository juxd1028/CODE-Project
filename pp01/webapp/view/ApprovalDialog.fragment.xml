<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog
        id="approvalDialog"
        title="{i18n>APPROVAL_DIALOG_TITLE}"
        contentWidth="460px"
        stretch="{device>/system/phone}"
    >
        <content>
            <VBox
                id="approvalForm"
                class="sapUiSmallMargin"
            >
                <!-- 오더 종류 -->
                <Label
                    id="lblAutyp"
                    text="{i18n>APPROVAL_TYPE_LABEL}"
                    required="true"
                />
                <Select
                    id="selAutyp"
                    selectedKey="{approval>/autyp}"
                    change="onApprovalTypeChange"
                    width="100%"
                >
                    <core:Item
                        id="itemAutypMs"
                        key="MS"
                        text="{i18n>KPI_PROD}"
                    />
                    <core:Item
                        id="itemAutypUr"
                        key="UR"
                        text="{i18n>KPI_URGENT}"
                    />
                    <core:Item
                        id="itemAutypRp"
                        key="RP"
                        text="{i18n>KPI_REPAIR}"
                    />
                </Select>

                <!-- 자재 코드 -->
                <Label
                    id="lblMatnr"
                    text="{i18n>LABEL_MATERIAL_CODE}"
                    required="true"
                    class="sapUiSmallMarginTop"
                />
                <ComboBox
                    id="cbMatnr"
                    selectedKey="{approval>/matnr}"
                    items="{approval>/materialList}"
                    selectionChange="onApprovalMaterialChange"
                    width="100%"
                    placeholder="{i18n>PLACEHOLDER_SELECT_MATERIAL}"
                >
                    <core:Item
                        id="itemMatnr"
                        key="{approval>matnr}"
                        text="{approval>matnr} - {approval>maktx}"
                    />
                </ComboBox>

                <!-- 자재명 (자동 표시, DB 미저장) -->
                <Label
                    id="lblMaktx"
                    text="{i18n>LABEL_MATERIAL_NAME}"
                    class="sapUiSmallMarginTop"
                />
                <Text
                    id="txtMaktx"
                    text="{= ${approval>/maktx} || '-' }"
                />

                <!-- 요청 원천 번호 -->
                <Label
                    id="lblVbeln"
                    text="{i18n>LABEL_SOURCE_NO}"
                    class="sapUiSmallMarginTop"
                />
                <Input
                    id="inpVbeln"
                    value="{approval>/vbeln}"
                    maxLength="10"
                    width="100%"
                    placeholder="{i18n>PLACEHOLDER_SOURCE_NO_OPTIONAL}"
                    showValueHelp="true"
                    valueHelpRequest="onVbelnValueHelp"
                />

                <!-- 아이템 번호 -->
                <Label
                    id="lblPosnr"
                    text="{i18n>LABEL_ITEM_NO}"
                    class="sapUiSmallMarginTop"
                />
                <Input
                    id="inpPosnr"
                    value="{approval>/posnr}"
                    maxLength="4"
                    width="100%"
                    placeholder="{i18n>PLACEHOLDER_ITEM_NO_OPTIONAL}"
                    showValueHelp="true"
                    valueHelpRequest="onPosnrValueHelp"
                />

                <!-- 생산 계획 기간 -->
                <Label
                    id="lblPeriod"
                    text="{i18n>LABEL_PLAN_PERIOD}"
                    required="true"
                    class="sapUiSmallMarginTop"
                />
                <DateRangeSelection
                    id="drsPeriod"
                    dateValue="{approval>/dateFrom}"
                    secondDateValue="{approval>/dateTo}"
                    displayFormat="yyyy-MM-dd"
                    valueFormat="yyyy-MM-dd"
                    width="100%"
                    placeholder="{i18n>PLACEHOLDER_DATE_RANGE}"
                />

                <!-- 오더 수량 -->
                <Label
                    id="lblMenge"
                    text="{i18n>LABEL_ORDER_QTY}"
                    required="true"
                    class="sapUiSmallMarginTop"
                />
                <HBox
                    id="hboxMenge"
                    alignItems="Center"
                >
                    <Input
                        id="inpMenge"
                        value="{approval>/menge}"
                        type="Number"
                        width="9rem"
                    />
                    <Text
                        id="txtGmein"
                        text="{= ${approval>/gmein} || '' }"
                        class="sapUiSmallMarginBegin"
                    />
                </HBox>
            </VBox>
        </content>

        <buttons>
            <Button
                id="btnApprovalSubmit"
                text="{i18n>BUTTON_CREATE}"
                type="Emphasized"
                press="onApprovalSubmit"
            />
            <Button
                id="btnApprovalCancel"
                text="{i18n>BUTTON_CANCEL}"
                press="onApprovalCancel"
            />
        </buttons>
    </Dialog>
</core:FragmentDefinition>
