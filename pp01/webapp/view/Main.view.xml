<mvc:View
    controllerName="code.t4.ui5.pp01.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:unified="sap.ui.unified"
    xmlns:core="sap.ui.core"
    displayBlock="true"
>
    <Page
        id="page"
        title="{i18n>title}"
        titleLevel="H2"
    >
        <content>
            <!-- ============================================ -->
            <!-- 상단 타일: 작업장별 가동현황 한눈에           -->
            <!-- ============================================ -->
            <HBox
                items="{view>/workCenterStats}"
                wrap="NoWrap"
                renderType="Bare"
                justifyContent="SpaceAround"
                class="sapUiSmallMargin wcHBox"
            >
                <Panel
                    class="wcCard"
                    backgroundDesign="Solid"
                >
                    <VBox
                        alignItems="Center"
                        width="100%"
                    >
                        <Title
                            text="{view>arbpl}"
                            level="H4"
                        />
                        <Text
                            text="{view>ktext}"
                            class="sapUiTinyText sapUiTinyMarginBottom"
                        />

                        <HBox
                            alignItems="Baseline"
                            class="sapUiSmallMarginTop sapUiSmallMarginBottom"
                        >
                            <Title
                                text="{view>today_qty}"
                                level="H1"
                                titleStyle="H1"
                            />
                            <Text
                                text="{= ' / ' + ${view>capac}}"
                                class="sapUiSmallMarginBegin"
                            />
                        </HBox>

                        <Text
                            text="{= ${i18n>STATUS_PLAN} + ' ' + ${view>schd} + ' · ' + ${i18n>STATUS_RUNNING} + ' ' + ${view>run} }"
                            class="sapUiTinyText sapUiSmallMarginBottom"
                        />

                        <ProgressIndicator
                            percentValue="{view>loadPct}"
                            displayValue="{view>loadPct}%"
                            showValue="true"
                            state="{= ${view>loadPct} > 100 ? 'Error' :
                         ${view>loadPct} >= 80 ? 'Critical' :
                         ${view>loadPct} > 0 ? 'Information' : 'None' }"
                            width="100%"
                        >
                            <layoutData>
                                <FlexItemData
                                    id="wcLoadIndicatorLayout"
                                    alignSelf="Stretch"
                                />
                            </layoutData>
                        </ProgressIndicator>
                    </VBox>
                </Panel>
            </HBox>

            <!-- ============================================ -->
            <!-- 공정 일정 캘린더 (SinglePlanningCalendar)     -->
            <!-- ============================================ -->
            <Panel
                id="calendarPanel"
                expandable="false"
                class="sapUiResponsiveMargin"
                width="auto"
            >
                <headerToolbar>
                    <OverflowToolbar id="calendarHeaderToolbar">
                        <Title
                            id="calendarTitle"
                            text="{i18n>CALENDAR_TITLE}"
                            level="H4"
                        />
                        <ToolbarSpacer id="calendarHeaderSpacer" />
                        <Button
                            id="refreshBtn"
                            tooltip="{i18n>BUTTON_REFRESH}"
                            icon="sap-icon://refresh"
                            press="onRefresh"
                        />
                        <Button
                            id="orderSearchOpenBtn"
                            text="{i18n>BUTTON_SEARCH}"
                            icon="sap-icon://search"
                            press="onOpenOrderSearch"
                        />
                        <Button
                            id="approvalOpenBtn"
                            text="{i18n>BUTTON_CREATE}"
                            type="Emphasized"
                            icon="sap-icon://add"
                            press="onOpenApprovalDialog"
                        />
                    </OverflowToolbar>
                </headerToolbar>
                <content>
                    <!-- 필터바: 작업장 / 상태 콤보박스 -->
                    <Toolbar
                        id="filterToolbar"
                        class="sapUiSmallMarginBottom"
                    >
                        <Label
                            text="{i18n>LABEL_WORKCENTER}"
                            labelFor="arbplSelect"
                        />
                        <Select
                            id="arbplSelect"
                            selectedKey="{view>/selectedArbpl}"
                            change="onFilterChange"
                            items="{view>/workCenterList}"
                        >
                            <!-- '전체' 항목은 arbpl='' 이라 ktext 만 표시되게 분기 -->
                            <core:Item
                                key="{view>arbpl}"
                                text="{= ${view>arbpl} === '' ? ${view>ktext} : ${view>arbpl} + ' - ' + ${view>ktext} }"
                            />
                        </Select>

                        <ToolbarSpacer />

                        <Label text="{i18n>LABEL_STATUS}" />
                        <Select
                            id="statuSelect"
                            selectedKey="{view>/selectedStatu}"
                            change="onFilterChange"
                        >
                            <core:Item
                                key="ALL"
                                text="{i18n>LABEL_ALL}"
                            />
                            <core:Item
                                key="SCHD"
                                text="{i18n>STATUS_PLAN}"
                            />
                            <core:Item
                                key="RUN"
                                text="{i18n>STATUS_RUNNING}"
                            />
                            <core:Item
                                key="DONE"
                                text="{i18n>STATUS_DONE}"
                            />
                        </Select>
                    </Toolbar>

                    <!-- 월간 캘린더 본체 -->
                    <SinglePlanningCalendar
                        id="opCalendar"
                        class="myCalendar"
                        startDate="{view>/startDate}"
                        appointments="{view>/filteredOperations}"
                        appointmentSelect="onAppointmentPress"
                        startDateChange="onCalendarDateChange"
                    >
                        <views>
                            <SinglePlanningCalendarMonthView
                                key="MonthView"
                                title="{i18n>CALENDAR_MONTH_VIEW}"
                            />
                        </views>

                        <appointments>
                            <unified:CalendarAppointment
                                title="{view>title}"
                                text="{view>text}"
                                tooltip="{view>tooltip}"
                                startDate="{view>startDate}"
                                endDate="{view>endDate}"
                                type="{view>type}"
                                color="{view>color}"
                            />
                        </appointments>
                    </SinglePlanningCalendar>
                </content>
            </Panel>

            <!-- ============================================ -->
            <!-- 현재 생산 중인 오더 리스트 (RUN)              -->
            <!-- ============================================ -->
            <Panel
                id="productionListPanel"
                expandable="false"
                class="sapUiResponsiveMargin"
                width="auto"
            >
                <content>
                    <List
                        id="productionTable"
                        items="{view>/productionList}"
                        growing="true"
                        growingThreshold="20"
                        growingScrollToLoad="false"
                        sticky="HeaderToolbar"
                    >
                        <headerToolbar>
                            <OverflowToolbar id="productionToolbar">
                                <Title
                                    id="productionTitle"
                                    text="{i18n>PRODUCTION_TITLE}"
                                    level="H4"
                                />
                                <ObjectStatus
                                    id="productionCount"
                                    text="{= ${view>/productionList}.length + ' ' + ${i18n>UNIT_PIECES} }"
                                    state="Warning"
                                    class="sapUiTinyMarginBegin"
                                />
                                <ToolbarSpacer id="productionToolbarSpacer" />
                                <SearchField
                                    id="productionSearch"
                                    width="18rem"
                                    placeholder="{i18n>PLACEHOLDER_SEARCH}"
                                    search="onProductionSearch"
                                    liveChange="onProductionSearch"
                                />
                            </OverflowToolbar>
                        </headerToolbar>

                        <items>
                            <CustomListItem
                                id="productionRow"
                                type="Active"
                                press="onOrderPress"
                            >
                                <VBox
                                    id="prodCardBox"
                                    class="sapUiSmallMarginBegin sapUiSmallMarginEnd sapUiSmallMarginTop sapUiSmallMarginBottom"
                                >
                                    <!-- 1행: 오더번호 · 라우팅번호 · 자재 -->
                                    <HBox id="prodRow1">
                                        <VBox
                                            id="prodC1a"
                                            width="30%"
                                        >
                                            <Label
                                                id="prodL1a"
                                                text="{i18n>COL_ORDER_NO}"
                                            />
                                            <ObjectIdentifier
                                                id="prodV1a"
                                                title="{view>Aufnr}"
                                            />
                                        </VBox>
                                        <VBox
                                            id="prodC1b"
                                            width="30%"
                                        >
                                            <Label
                                                id="prodL1b"
                                                text="{i18n>COL_ROUTING_NO}"
                                            />
                                            <Text
                                                id="prodV1b"
                                                text="{view>Plnnr}"
                                            />
                                        </VBox>
                                        <VBox
                                            id="prodC1c"
                                            width="40%"
                                        >
                                            <Label
                                                id="prodL1c"
                                                text="{i18n>COL_MATERIAL}"
                                            />
                                            <ObjectIdentifier
                                                id="prodV1c"
                                                title="{view>Matnr}"
                                                text="{view>Maktx}"
                                            />
                                        </VBox>
                                    </HBox>

                                    <!-- 2행: 수량 · 상태 · 생산시작일 -->
                                    <HBox
                                        id="prodRow2"
                                        class="sapUiSmallMarginTop"
                                    >
                                        <VBox
                                            id="prodC2a"
                                            width="30%"
                                        >
                                            <Label
                                                id="prodL2a"
                                                text="{i18n>COL_QTY}"
                                            />
                                            <ObjectNumber
                                                id="prodV2a"
                                                number="{view>Done_qty} / {view>order_qty}"
                                                unit="{view>Gmein}"
                                                emphasized="true"
                                            />
                                        </VBox>
                                        <VBox
                                            id="prodC2b"
                                            width="30%"
                                        >
                                            <Label
                                                id="prodL2b"
                                                text="{i18n>COL_STATUS}"
                                            />
                                            <ObjectStatus
                                                id="prodV2b"
                                                text="진행중"
                                                state="Warning"
                                            />
                                        </VBox>
                                        <VBox
                                            id="prodC2c"
                                            width="40%"
                                        >
                                            <Label
                                                id="prodL2c"
                                                text="{i18n>LABEL_START_DATE}"
                                            />
                                            <Text
                                                id="prodV2c"
                                                text="{path: 'view>Gstri', type: 'sap.ui.model.type.Date', formatOptions: { pattern: 'yyyy-MM-dd' }}"
                                            />
                                        </VBox>
                                    </HBox>
                                </VBox>
                            </CustomListItem>
                        </items>
                    </List>
                </content>
            </Panel>
        </content>
    </Page>
</mvc:View>
